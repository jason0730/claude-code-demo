apiVersion: v1
kind: Secret
metadata:
  name: api-server-secret
  namespace: api-server
type: Opaque
stringData:
  jwt-secret: "your-super-secret-jwt-key-change-in-production"
  # 在生产环境中，应该使用外部密钥管理系统（如 HashiCorp Vault）
  # 或者使用 kubectl create secret 命令创建
